(*exemple de code avec if-then-else pour tester la génération du code asml et arm*)
let a = 5 in
  let b = 0 in
    let c = 3 in
      if (a - c) >= b then
        print_int(a - c)
      else
        print_int(b)